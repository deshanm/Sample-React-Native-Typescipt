// const useFilterChannels = (channels: ChatChannelType[], from: string) => {
//   const filteredList = channels.filter((obj) => {
//     if (obj.user) {
//       const name = obj.user.name;
//       return name.includes(from);
//     }
//     return false;
//   });
//   return [filteredList];
// };
